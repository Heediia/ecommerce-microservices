apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: ecommerce
spec:
  selector:
    app: notifications

  ports:
    - protocol: TCP
      port: 3006
      targetPort: 3006

  type: ClusterIP