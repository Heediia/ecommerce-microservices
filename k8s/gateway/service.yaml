apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: ecommerce
spec:
  selector:
    app: gateway

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

  type: ClusterIP