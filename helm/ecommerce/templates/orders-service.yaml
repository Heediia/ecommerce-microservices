apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: orders

  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004

  type: ClusterIP