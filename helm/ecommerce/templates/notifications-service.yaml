apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: notifications

  ports:
    - protocol: TCP
      port: 3006
      targetPort: 3006

  type: ClusterIP