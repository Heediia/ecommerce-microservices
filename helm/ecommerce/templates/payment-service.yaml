apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: payment

  ports:
    - protocol: TCP
      port: 3005
      targetPort: 3005

  type: ClusterIP