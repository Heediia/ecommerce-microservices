apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: auth

  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003

  type: ClusterIP